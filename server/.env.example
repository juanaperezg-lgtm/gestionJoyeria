# Puerto (Render lo asigna automáticamente con la variable PORT)
PORT=3001

# Base de datos SQLite
DATABASE_URL="file:./dev.db"

# JWT
JWT_SECRET="secreto_seguro_cambiar_en_produccion"

# Render asigna esta variable automáticamente — NO la configures manualmente
# Se usa para el mecanismo anti-sleep (auto-ping cada 14 min)
# RENDER_EXTERNAL_URL=https://tu-backend.onrender.com
